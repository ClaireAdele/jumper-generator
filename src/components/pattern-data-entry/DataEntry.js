import "../../App.css";

const DataEntry = () => {
    // chest measurement + ease
    // shoulder measurements
    //pattern repeat?? Could do with that too maybe?
    //gauge of yarn in rows 
    // body length, from bottom of jumper to armhole
    //from armhole to bottom of neckline
    return (
      <div className="pageBackground">
        <div className="pageShaper"></div>
      </div>
    );
};

export default DataEntry;
